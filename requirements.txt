Flask
python-dotenv
openai
image-resizor  # Replace with actual package name if this isn't correct
Werkzeug
Pillow  # If you're dealing with image processing
sqlite3  # Included by default with Python, but no need to add it to requirements
# If you use `secrets` (part of Python standard library), you don't need to add it
